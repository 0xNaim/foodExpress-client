import CartView from "../../components/cart/CartView";
import Layout from "../../components/layout";

const Cart = () => {
  return (
    <Layout>
      <CartView />
    </Layout>
  );
};

export default Cart;
