import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#078852',
    },

    secondary: {
      main: '#19a969',
    },
  },
});

export default theme;
